/*

    const body = {objects}

    await fetch(apiUrl + '/endpoint',{
    
        method: 'POST',
        headers:{ 'Content-Type': 'application/json' },
        body: JSON.stringidy(body)
        
    });


    url is going to be the apiurl + the query parameters

    await fetch(url,{
    
        method: 'GET',
        headers:{ 'Content-Type': 'application/json' }
        
    });


    <input type="text" placeholder="Enter something..." value={inputValue} onChange={handleInputChange}/>
    <button onClick={handleSubmit}>Submit</button>
    <div><strong>Current Value:</strong> {inputValue}</div>
    */