/*
    Create a db.js to hold database connectivity
    Install pg library
    Create a env file and install dotenv library
        require('dotenv').config();
        const pool = require("./db");
    add gitignore file
    Begin working on a library app
*/